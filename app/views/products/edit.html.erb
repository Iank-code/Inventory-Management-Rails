
  <div>
    <h1 class="font-bold text-[100px]">Editing product</h1>
    <%= turbo_stream_from 'edit_product' %>
    <%= render "form", product: @product %>

    <br>

    <div>
      <%= link_to "Show this product", @product, class: 'bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-300' %>
      <%= link_to "Back to products", root_path, class: 'bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-300' %>
    </div>
  </div>
